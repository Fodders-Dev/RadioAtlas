# RadioAtlas: рабочая схема Codex + Claude

Обновлено 11.09.2026. Это короткая передача текущей работы, не новый источник
продуктовых требований. Требования — в PRODUCT.md и DESIGN-DIRECTION.md,
приоритет — в PLAN.md.

## Текущая передача: «Журнал A4» опубликован для пробы, доводка идёт

**Раунд 3, 11.09 вечер (Claude, `RadioAtlas-codex`, `codex/calm-home`):**
- ⚠ Регрессия сборки, найденная при проверке веера на проде: calm-Глобус
  приходил слушателям БЕЗ `GlobeExplorer-*.css` (все контейнеры `position:
  static`, экран 4,5k px). Причина — два `import()` в одном тернарном
  выражении `loadGlobeScreen`: Vite прикрепил к обёртке preload только
  классического Глобуса. `c2e3891` разносит импорты по своим стрелкам, страж
  `screenLoaders.test.ts`; выложено отдельно (юнит/типы локально, гейт CI
  перед деплоем), на проде подтверждено веером настоящими нажатиями.
- `eb0d707` контурные нав-иконки макета (home/globe/feed/library + искра
  Лиры) с точкой активного; «Ещё» у «Под настроение» → лист «Музыкальные
  истории» со всеми подборками (тап открывает лист подборки); «Моё» и
  «Поиск» без классического топбара, с журнальной шапкой (настройки, аккаунт,
  «Находки N · Источники N · Подборки N»; у Поиска — «‹» на Главную), флаг
  Settings перенесён в shell (`settingsOpen`); все функции Медиатеки на месте,
  вкладки на телефоне держат сегментную раскладку, ниже 360px — скроллятся.
- `e6d024f` и далее: листы Настроек/Аккаунта на бумаге журнала.
- Проверка веера на опубликованном интерфейсе настоящими нажатиями мыши
  (Playwright против https://radioatlas.ru, 390×844): Стокгольм → кучка →
  веер 42 → листы 0/1/2 → три разных названия (Country, Rock, Urban Flow),
  веер остаётся открытым, `audio.src` пуст до Play; Play на карточке → Urban
  Flow «подключаем»/«Буферизация» (прямой https-поток).
- Сверка экранов: Главная, Лента, Глобус, Лира, Моё, Поиск, листы Настроек,
  Аккаунта, Theme Studio (бумага + тёмная карточка текущей темы — по замыслу).
  Намеренно оставлено: строка Лиры на Главной без выдуманного утверждения; фон
  станции в Ленте вместо слова «écoute.».
- Проверки раунда: `typecheck`/`typecheck:test` exit 0; webapp unit 855
  passed (105 файлов, + `screenLoaders.test.ts`); `calm-home.spec.ts` (+ тест
  «journal shell») и `calm-globe.spec.ts` — 18 passed; полный функциональный
  Playwright — 306 passed; push `104840a` в `master`. CI-гейт упал на новом
  спеке «journal shell» (локатор ждал `<audio>`, которого до Play нет —
  локально элемент был), поправлено чтением через DOM и отправлено следом.
- Ловушка встроенного браузера: при эмуляции 320 `innerWidth` = 352, и
  fixed-элементы (нав) измеряются шире экрана — это не переполнение
  приложения (`.app-shell-v2` = 320, `clientWidth` = 320).


**Пробная публикация и доводка, 11.09 день (Claude, `RadioAtlas-codex`, `codex/calm-home`):**
на проде **`cc082e5`** — https://radioatlas.ru/?calm=1 (без `?calm=1` прежний
интерфейс). Точка отката `4415576`. Деплой: push в `master` → CI 34616687419
→ `deploy-server.yml` 34616687456, оба зелёные; сборка на проде
`index-DOrVvzp2.js`. Проверено на опубликованной HTTPS-версии в браузере на
390: Главная-журнал открывается; «Включай» → Antenne Bayern играет через
`/api/stream` (audio readyState 4, currentTime растёт, ICY-трек в
мини-плеере); переключение на JazzWorks (прямой https) — играет; отказ:
Atlantida Radio Online (прокси 502 → audio error 4) → тост; «Включай» после
отказа снова играет. Медленный MGL Radio: 24 с «Буферизация», потом честный
`playing`.

Дефект, найденный на проде: после отказа станция исчезала из мини-плеера
(и на Поиске, и на Главной) — нет ни «Не удалось подключиться», ни «Повторить».
Локально не воспроизводилось (на dev:local прямой https-кандидат падает без
асинхронного хода по кандидатам). Причина и правка — `c270b4f`
(`useAudioPlayer.ts`, ref запрошенной станции захватывается до хода). Ждёт
второй публикации и повторной проверки на проде.

Локальные коммиты после публикации: `6580d31` (тексты Главной, ротация
обложки), `48e79e6` (жанровые цвета + веер на Глобусе, API `genre` в points),
`c270b4f` (плеер, тост, подписи историй, лицо Лиры). Подробности в PLAN.md
«Next».

Сравнение с макетом бок о бок (макет `node docs/prototypes/directions/serve.cjs`
→ http://127.0.0.1:4180/journal.html?screen=home|feed|globe|lira, приложение
http://127.0.0.1:5184/?calm=1, оба на 390):
- Главная: заголовок, поиск/палитра, строка Лиры, ведущая история, «Начните с
  этих эфиров», «Под настроение», тайлы стран, «Вокруг света», жанры, находки,
  сообщество, мини-плеер — совпадают. Исправлено: кикер над заголовком в
  подписи истории; лицо Лиры. Оставлено намеренно: строка Лиры в макете
  «Сегодня — чуть больше джаза» (выдуманное утверждение) → «Спросить о
  музыке». Открыто: «→ Ещё» у «Под настроение» (в приложении все истории
  в рельсе), заливка активной нав-иконки (в макете контурные).
- Лента: экран на станцию, статус, страна → Глобус, трек + закладка,
  «Слушать эфир» с волной, рейка (сердце/Лира/ещё), шаги, таймер — совпадают;
  громкость — в лотке «Ещё» (`FeedPlayerTools`), как в макете. Оставлено
  намеренно: фон станции вместо слова «écoute.» (декоративный французский).
- Глобус: совпадает; вместо строки «Точка — эфир · число — группа» — легенда
  жанров словами.
- Лира: лицо, чипы, карточки источников с Play и «Показать на глобусе» —
  совпадают; в макете показан готовый диалог, в приложении настоящий ответ
  (на dev:local AI выключен — честная ошибка связи).
- Моё: в макете экрана нет; приложение показывает существующую Медиатеку с
  классическим топбаром — расхождение материала, открыто.

**Вторая и третья публикации (11.09 вечер):** `27f60b8` (тексты Главной,
жанры + веер, плеер/тост/подписи/лицо Лиры, передача) — CI 34621385511 и
деплой 34621385514 зелёные, сборка `index-X2vdZkLt.js`; проверено на проде:
отказ Atlantida → мини-плеер остаётся «Не удалось подключиться» с кнопкой
«Повторить», тост «Поток сейчас недоступен. Попробуй другую станцию.», Главная с
новой обложкой дня и лицом Лиры. Затем `8671b4b` (тап по выбранной станции в
кучке раскрывает кучку; страна без координат предлагает список каталога —
Монголия: 9 станций, лист открывается с первой страницей) — полный
функциональный прогон 305 passed, push выполнен, деплой см. `gh run list`.
Точка отката по-прежнему `4415576` (до всей серии) либо `cc082e5` (первая проба).

Проверки этого шага: `typecheck`, `typecheck:test` — exit 0; `test:api` —
exit 0 (+ `catalog.genreFamily.test.ts`); webapp unit — 854 passed (104
файла); `calm-home.spec.ts` 11 passed, `calm-globe.spec.ts` (+ «pile» с
веером) и `globe-drag.spec.ts` — 17 passed вместе с calm-home; полный
функциональный Playwright перед первой публикацией — 303 passed, 7 skipped
(лог `e2e-full3.log`); перед второй публикацией — 304 passed; перед третьей —
305 passed; четвёртый прогон поймал флак спека «pile» (тап во время перелёта
камеры прерывает ease и оставляет группу мимо центра) — спек теперь ждёт
посадки по счётчику `data-moves`/`data-camera` на хосте карты, 4/4 подряд.

Проверка Глобуса на реальных данных (скрипт Playwright против dev:local,
`scratchpad/real-pile.cjs` сессии, снимки 390×844): Монголия — «0 эфиров»,
подпись «Без точных координат: 9», «Открыть список» → лист страны с 9
станциями; Стокгольм — выбор Relax & Meditation, зум до улиц, тап по кучке →
веер из 24 листов спиралью с линиями к якорю, список «42 эфира» с жанрами
словами, панель возвращается к нормальной высоте (иначе верх веера уходил под
«Искать здесь»), тап по листу выбирает станцию, звук не запускается.

Найдено на проде после третьего среза: строки Глобуса без слов жанра, хотя
`/api/catalog/points` уже отдавал `genre` — клиент держит points в IndexedDB
сутки под ключом `points:v3`; ключ поднят до `points:v4` (как при переходе на
schemaVersion 3), иначе каждый слушатель видел бы нейтральные точки день.

Известные ловушки этого дня: правка `useAudioPlayer.ts`/`RadioContext.tsx`
под открытым dev:local роняет приложение по HMR («usePlayback must be used
inside RadioProvider») — перезагрузка лечит, это не продуктовый баг; Vite
однажды отдал устаревший transform `CalmHome.tsx` (без нового импорта) —
`touch` файла лечит. Синтетические клики по canvas из встроенного браузера
попадают в ореол выбора, а не в кучку под ним — см. открытый пункт в PLAN.md.

Четвёртый срез `ccbc8c4` (полный прогон 305 passed) отправлен в `master`
следом; после его деплоя цветные точки на проде видны всем без ожидания суток.

Следующее действие: проба владельцем на телефоне (https://radioatlas.ru/?calm=1);
открытые пункты паритета — нав-иконки, «→ Ещё» у «Под настроение», топбар
Медиатеки — и решение о переносе по умолчанию без `?calm=1`.

## Предыдущая передача: цельный «Журнал A4» работает в приложении, дизайн ещё не принят

**Интеграция всего варианта A4, 11.09 ночь (Claude, `RadioAtlas-codex`, `codex/calm-home`):**
http://127.0.0.1:5184/?calm=1 — Главная-журнал, вкладка «Лента» (вертикальная,
одна станция на экран), Глобус A4, окно Лиры и навигация Главная · Глобус ·
Лента · Лира · Моё; тема «Журнал» по умолчанию, остальные темы переключаются в
Theme Studio (иконка палитры на Главной). Что именно подключено — в PLAN.md.

Маршрут, пройденный в браузере на 390 (тема «Журнал», dev:local): Включай →
лист «Джаз без галстука» (3 загруженных источника + продолжение каталога,
«Ещё станции» 6 → 30, «В каталоге: 1534») → карточка источника Le Grigri →
«Показать на карте» → Глобус открыл Францию (у станции нет точных координат —
тост об этом, страна выбрана) → «Лента» из нав-бара на станции в эфире →
«Спросить Лиру» (чат в бумажном виде, вопрос ушёл, честная ошибка связи, AI на
dev:local выключен) → «Mexico →» из Ленты → Глобус на точку EXA FM с карточкой
→ «Моё» (существующая Медиатека, находки/избранное) → обратно на Глобус.
Осмотрены 320 и 390; темы «Журнал», neon, classic (→ «Журнал»). Исправлены по
дороге: паддинги кнопок Главной (сброс `:where`), ширина чипов ≥ 44 px,
переполнение имени станции в Ленте на 320 (`minmax(0,1fr)`), автозапуск чужой
станции при перезагрузке прямо в Ленту.

Настоящий звук в этом проходе: станции Home-surface с https-потоками на
http-странице dev:local не подключаются (CORS/сертификат) — интерфейс честно
показывает «Не удалось подключиться»/«Повторить»; SomaFM Secret Agent играл
(проверено ранее в этой же ветке). Находка с настоящим треком в Ленте
проверена моком (e2e) и ранее вживую в мини-плеере. Физический телефон и iOS-
фон не проверялись. Push/deploy не выполнялись.

Проверки: `typecheck`, `typecheck:test` — exit 0; webapp unit — 850 passed
(104 файла, в том числе новые `calmStories.test.ts`, паритет локалей, контраст
тем с новой «Журнал»); `tests/calm-home.spec.ts` переписан под новую Главную
(«Включай»+находка на 320/390/411 × «Журнал»/aurora, восстановленная станция
без автозапуска из мини-плеера и из нав-бара, Лента с таймером и шагами,
история → каталог по страницам → источник → Глобус, повтор после 502);
`tests/calm-globe.spec.ts` обновлён. Полный функциональный Playwright
(`PLAYWRIGHT_SKIP_PIXELS=1`, serial): 300 passed, 7 штатных skip, 1 упал —
`mobile.spec.ts` «theme defaults expose bundled shell themes» ожидал прежний
список встроенных тем; список дополнен «journal», спека 3/3 passed повторно.
Pixel baselines не перерисовывались (осознанный редизайн, приёмка за
владельцем).


**Интеграция A4, 11.09 вечер (Claude, `RadioAtlas-codex`, `codex/calm-home`):**
экран Глобуса A4 перенесён в приложение за `?calm=1` — http://127.0.0.1:5184/?calm=1
→ вкладка «Глобус». Это рабочая интеграция, не макет: настоящие points каталога
(только реальные координаты), `fetchStationById` перед каждым Play, существующие
`playStation`/`toggle`, сердце в библиотеку, закладка через `copyTrack`,
Лира через ChatSheet (вопрос уходит настоящему ассистенту; на dev:local AI
выключен, и чат честно показывает ошибку связи). Прежний прицельный Глобус
остаётся по умолчанию без `?calm=1`. Макет на 4180 больше не нужен для пробы,
но сохранён как история (`docs/prototypes/directions/README.md`).

Что проверено: `typecheck` и `typecheck:test` — exit 0; webapp unit — 846
passed; `tests/calm-globe.spec.ts` — 4 passed (путь: список страны → имя →
карточка 140 px → Play → «слушаем» → сердце → подробности 276 px → находка
«Mock Song» в библиотеке → жест сворачивания → список с активной строкой →
Мир → выбор страны → поиск → «Больше карты» → перемещение без смены списка →
«Искать здесь»; Лира получает вопрос об источнике; Главная «· На карте» ведёт в
ту же страну; 320/светлая — без переполнения, все цели ≥ 44 px). Полный
функциональный Playwright (`PLAYWRIGHT_SKIP_PIXELS=1`, serial) — 311 passed,
7 штатных skip, exit 0, 7,2 мин. Pixel baselines не перерисовывались.
Осмотрено в браузере на 390 и 320, тёмная и светлая (pastel) темы.
**Настоящий звук:** SomaFM Secret Agent из списка Глобуса на dev:local —
`currentTime` 6,8 с, `readyState` 4, мини-плеер показал настоящий трек с
закладкой. Станция Radiorama Sonora не подключилась (https-поток без прокси на
http-странице) — карточка показала «Не удалось подключиться» и «Повторить».
Физический телефон и iOS-фон не проверялись. Push/deploy не выполнялись.

Следующее действие — проба владельцем на телефоне; затем решение, становится
ли композиция A4 Глобусом по умолчанию.

**Предыдущая редакция A4, 11.09:** владелец оценил A3 в 7/10, одобрил крупные
точки, попросил освободить карту от высокой карточки и хочет пробовать продукт.
Карточка источника теперь 140 px (было 302); имя, Play/Pause, сердце и Лира
на виду, подробности раскрываются. Список начинается с 192 px, поиск и большая
панель — по запросу. Заголовок можно потянуть вверх/вниз; кнопки сохраняются.
Проверен источник из присланного скриншота — Радіо Хартія (Radio Khartia).
Переноса в app и deploy пока нет: это прежний изолированный прототип на 4180.
A3 — локальная точка возврата `ab7dedd`. Следующий этап — интеграция с реальными
аудио/находками/Лирой и проба; не выдавать макет за готовый production экран.
Главное продуктовое замечание: станции — источники; ценность — музыкальные
находки, которые сохраняются без выхода из эфира. Прототип этого ещё не
доказывает, потому что трек — пример, AI — сценарий.
Проверены 320/390, свет/ночь, длинное имя, жест раскрытия, Play/Pause, сердце,
переход к Лире и сохранение эфира. Оба typecheck и tsc виджета — exit 0.

**Предыдущая редакция A3, 11.09:** владелец назвал A2 красивым, но удобство — 6/10.
Дизайн ещё не принят. Доработаны плотность Главной и Лиры, связка карты и
станций: числа раскрывают группы, название показывает станцию, Play включает.
Панель списка разворачивается/сворачивается. После движения карты список
остаётся прежним до «Искать здесь»; страна задаёт начало, не запирает географию.
Вертикальная Лента и разделение сердца/закладки сохранены.

Открыть http://127.0.0.1:4180/; пары Главная/Глобус и Лента/Лира, 320/390/411×844.
`journal.html` — A3; `a2.html` / `journal-a2.html` — сохранённый A2 (`139133d`);
`comparison.html` — первоначальные А/Б. Из этой копии запустить
`npm.cmd run dev:local` (Vite 5184/API 4341, stores `.tmp/dev-local`), затем
`node docs/prototypes/directions/serve.cjs` (4180). Обе команды оставлены работать.
Сервер собирает два отдельных виджета в `.tmp/directions/`: адаптер исходного
Globe для A2 и изолированное исследование кластеров MapLibre для A3. В обоих
случаях использованы существующие зависимости, геометрия и Esri; app не изменён.
Аудио моделируется, трек — пример, AI-ответы — явно подписанный сценарий.
Не переносить в основное приложение, не push/deploy до решения владельца.
Проверены реальные группы, поиск, выбор/Play, страна, перемещение и явный поиск
в области, размеры панели, 320/390 px, тёмная тема. Оба typecheck — exit 0.
Следующее действие — **оценка владельцем A3**. Детали — в README макетов.

**Предыдущая передача 11.09 (`76e0aed`):** два цельных интерактивных макета вынесены в
`docs/prototypes/directions/`, вне основного приложения. **А · Журнал** —
музыкальные истории; **Б · Атлас** — исследование страны и её звучания.
Сравнение: http://127.0.0.1:4180/comparison.html. Запуск из этой копии:
`node docs/prototypes/directions/serve.cjs`; для живого продолжения каталогов
нужен существующий preview/API на 5184. В сравнении — одинаковые телефоны
320/390/411×844, общий выбор Главных/Лент/таймеров/тем и независимая прокрутка.

Это макеты **без аудио**, с реальными станциями и явно подписанным примером
трека. В них работают сценарии сохранения, очереди, таймера, темы и редактор
личного фона; аккаунт, AI и Theme Studio приложения не интегрированы.
Исходники приложения и соседняя копия Claude не изменялись. Обнаруженный
скачок прокрутки при возврате в Атлас исправлен сохранением списка страны.
Оба typecheck — exit 0; браузерный проход и границы записаны в
`docs/prototypes/directions/README.md`. Полные suites приложения не запускались.
Эта передача завершилась предпочтением А; дальнейший статус описан выше.

Предыдущее решение владельца 11.09: результат `2f44a1c` слишком узкий по
видимому эффекту. Следующее действие — два существенно разных цельных варианта
Главной, Ленты-плеера и навигации для сравнения до переноса в приложение.
Не продолжать мелкую полировку существующей композиции как будто она принята.
Рабочая ветка `codex/calm-home`, папка `RadioAtlas-codex`; соседнюю `Radio++_tg`
с работой Claude не менять. Документы с этим уточнением идут после `2f44a1c`.

Постоянные решения владельца собраны в [DESIGN-DIRECTION.md](DESIGN-DIRECTION.md)
и включены в обязательное чтение CLAUDE.md. Apple — ориентир композиции,
пространства, взаимодействия и материалов. Тепло, музыкальность, выразительное
стекло и богатое исследование обязательны. Присланную картинку Лиры не вставлять.

Ночная итерация 11.09: тёмный глобус заменён оригинальной тёплой стеклянной
звуковой волной (`sound-glass.webp`, 34.5 KB). Подборки подняты выше стран;
управление отделено от содержимого материалом, бликами и глубиной.

Страны, жанры и четыре тематические подборки продолжаются из полного каталога
страницами по 30 через `/catalog/search`. Новое поле `mood` использует те же
точные теги, что серверные mood rails. Несуществующий mood — HTTP 400. Кеш
включает фильтр и стабильный seed; Home не скачивает полный fallback-каталог
при ошибке. Выбор страны открывает нативный dialog с поиском, полным списком
стран каталога, Escape и возвратом фокуса. Прежний предел facets в 80 снят.

Выбор направления, страницы, горизонтальная прокрутка и положение Главной
сохраняются при раскрытии/закрытии Ленты. Память живёт только в текущей SPA-сессии
и сбрасывается новой сессией Home или reload; содержит фильтры и публичные станции,
не аккаунт и не находки. Поздний ответ не попадает в другую выбранную страну.
Сетевая ошибка оставляет показанные станции и повтор. Эти действия не меняют звук.

Живой локальный прогон: вечерняя подборка — 1 140 станций, country facets —
240 записей стран/территорий. Это наблюдение 11.09, не константы продукта.
Личные коллекции берутся из библиотеки; публичная витрина сообщества ещё не
реализована и не подменяется вымышленными авторами.

Страница утреннего просмотра: `output/design-review/index.html` (локальная,
не коммитится); снимки — `output/playwright/calm/`. Встроенные темы сняты на
реальном каталоге; тестовые кадры плеера и Theme Studio подписаны как фикстуры.
Следующий шаг: сравнение двух новых композиций по DESIGN-DIRECTION.md §6.
Дизайн не объявлен принятым, production не изменялся. Моки не доказывают iPhone audio.

Финальные проверки ночного среза: source typecheck и test typecheck — exit 0;
API — 585 passed; webapp unit — 837 passed; полный функциональный Playwright —
307 passed, 7 штатных skip, exit 0 (6.9 мин); build — exit 0. Отдельно проверены
отказ/повтор догрузки, поздний ответ после смены страны, отсутствие смешения mood
кеша, сохранение обоих scroll offsets после Ленты. В браузере проверены Escape
и возврат фокуса из выбора страны, reduced transparency/motion и отсутствие
горизонтального переполнения. Функциональная матрица включает 320/390/411,
встроенные светлую/тёмные темы и пользовательскую картинку через Theme Studio.
Pixel baselines не обновлялись; приёмка внешнего вида остаётся за владельцем.

## Где работать

| Кто / назначение | Рабочая копия | Ветка |
| --- | --- | --- |
| Сохранённая работа Claude, общая база | `../Radio++_tg` | `codex/design-checkpoint-20260910` |
| Следующий срез Codex | `../RadioAtlas-codex` | `codex/calm-home` |
| Production baseline до этих локальных изменений | — | `master`, `0c8be87` |

WIP-снимок **d768aba** сохраняет существовавшие на момент передачи правки
Claude (`AirBlock`, стили, локали, тесты), опись переноса и calm-прототип Codex.
Не считать его автоматически принятым редизайном или готовым к выкладке.
Существующие сторонние worktree (аудиофикс, Лира, Claude) не изменялись.

Рабочие копии имеют отдельные файлы, индексы, node_modules и `.tmp`-данные.
Одновременно не редактировать одни файлы в одной копии. Ветку master не
использовать для незавершённой работы: push в неё запускает production deploy.
Checkpoint и настройка окружения остаются локально, без push.

## Запуск

В корне выбранной рабочей копии:

```powershell
npm.cmd ci                     # один раз; собственные зависимости worktree
npm.cmd run dev:local -- --check # проверить зависимости и свободные порты
npm.cmd run dev:local           # API + Vite, Ctrl+C останавливает оба
```

Приложение: **http://127.0.0.1:5184/**.
Старый эксперимент AirBlock: **http://127.0.0.1:5184/?air2=1**.
Новый срез Calm с реальным каталогом: **http://127.0.0.1:5184/?calm=1**.
API: `127.0.0.1:4341`; Vite проксирует `/api` именно сюда.
Каталог — из существующего локального snapshot; аккаунт/журнал/cache пишутся
в `.tmp/dev-local` этой рабочей копии. AI, billing и тестовая авторизация
выключены. Ключи и production `.env` для этого запуска не нужны.
Vite обновляет UI на лету; после изменения API перезапустить команду.

Для второго одновременного dev-запуска задать другую пару портов:

```powershell
$env:RADIO_DEV_WEB_PORT = '5185'
$env:RADIO_DEV_API_PORT = '4342'
npm.cmd run dev:local
```

Прототип отдельно: `npm.cmd run dev:prototype` → http://127.0.0.1:4179.
Переносимый HTML: `npm.cmd run build:prototype` →
`docs/prototypes/calm/radioatlas-calm.html`. Это **макет без звука**, с четырьмя
фикстурами. Оформление прототипа не является интеграцией с Theme Studio.
Адреса loopback работают на этом компьютере, не на телефоне.

## Режим и инструменты

`.codex/config.toml` задаёт High для проекта; явный выбор в задаче имеет свой
приоритет. Модель и глобальные permissions не меняли. Medium — небольшие
известные правки; Extra High — трудная диагностика/ревью. Максимум постоянно
не нужен. Подагенты — только при явной авторизации конкретной параллельной работы.

Новых платных интеграций и фоновых автоматизаций не установлено.
На этой машине при настройке: Node **24.12.0**, npm **11.6.2**; требование проекта
Node ≥24 выполнено. CI фиксирует Node 24.19.x: локальный прогон не заменяет
проверку CI того же коммита перед production. Системный Node не переустанавливали.

## Проверки

Во время итерации — соответствующий небольшой тест/браузерный сценарий.
Перед готовностью реализации — оба typecheck и затронутые suites по CLAUDE.md:

```powershell
npm.cmd run typecheck
npm.cmd run typecheck:test
npm.cmd --workspace apps/webapp run test:unit
$env:PLAYWRIGHT_SKIP_PIXELS = '1'
npm.cmd --workspace apps/webapp run test:e2e:functional
```

Каждую команду оценивать по собственному коду выхода. Полный функциональный
Playwright запускать **по одному на машину**: API-порт 4311 сейчас фиксирован
в конфиге. Dev-пара 5184/4341 с ним не пересекается. Не расширять допуски ради
зелёного прогона. Визуальный результат смотреть, а не принимать по числам.

### Проверено при настройке 10.09.2026

- База настройки: `6997c4b`. `npm ci` выполнен в отдельной RadioAtlas-codex.
- Оба typecheck завершились с кодом 0. Скриптовые тесты: 80 успешно,
  один штатный skip (`rsync` отсутствует на Windows); запуск Git Bash исправлен.
- В новой копии запущены настоящие API + Vite. `/api/health` вернул JSON
  `{"ok":true}`, `/api/catalog/summary` — каталог; Главная открылась в Chromium
  на 390×844. После Ctrl+C оба порта снова свободны.
- Это проверка окружения, не приёмка редизайна или звука. Внешние metadata-запросы
  отдельных станций дали CORS/404/502; один адрес разрешился в `198.18.x.x` и был
  отклонён защитой API. Защиту не ослабляли. Полный Playwright и фон на iPhone
  в рамках настройки не прогонялись.

## Первый срез реализации

**Результат:** открыл приложение → включил эфир → сохранил понравившийся трек,
не покидая спокойную Главную. Работает реальный RadioContext и модель находок.

Входит:

- Композиция Главной из calm-прототипа с настоящим каталогом и локалями.
- Один компактный плеер, явная закладка трека; сердце станции — отдельное действие.
- Сохранение без перестройки страницы/смены станции и с неблокирующим подтверждением.
- Существующая навигация и доступ к расширенным действиям сохраняются.
- Реальные токены тёмной/светлой темы и пользовательское оформление из Theme Studio.
- Новая композиция за выключенным по умолчанию флагом; прежний путь сохраняется.

Не входит: новый ранжировщик, рекомендации по треку из жанров станции,
интеграция Лиры с находками, новый Глобус, запись эфира, новый аудиодвижок,
новая архитектура аудиоплеера или расширение возможностей Theme Studio.

Приёмка: первый запуск и восстановление без ложного playing, запуск/пауза,
подключение/ошибка/нет metadata, поимка с прежним stationId и временем,
отсутствие сдвига Home/потери последней строки, ширины 320/390/411,
контраст и кликабельность при своей картинке. Мок не доказывает звук:
настоящий iPhone background-проверяется отдельно перед выкладкой.

### Реализация для просмотра

`CalmHome` получает существующую рекомендацию/станции Home и фиксирует их на
время посещения. `CalmMiniPlayer` использует тот же RadioContext, проверку
достоверности названия и `copyTrack`; отдельного аудиоплеера/хранилища нет.
Закладка исчезает без названия, при ошибке остаётся повтор. Нажатие на информацию
в мини-плеере открывает Ленту на текущей станции; дополнительные действия —
в её лотке «Ещё», таймер доступен также кнопкой в шапке Ленты.
Поиск по настроению открывает существующий Search, «Листать эфиры» — Ленту;
переход сам по себе не переключает эфир. Находки открывают вкладку tracks.

`tests/calm-home.spec.ts` проверяет первый запуск, сохранение при отказе clipboard,
неподвижность разделов, размеры/доступность нижней строки на 320/390/411 в двух
темах, восстановление, подключение, ошибку/повтор, отсутствие metadata и собственную
картинку через настоящий редактор Theme Studio. Измерение ждёт шрифт и конечную
анимацию. Искусственные error/buffering изолируют сетевой WAV от медиа-мока:
иначе браузер способен сам послать playing, противореча имитируемому отказу.
Пробная правка useAudioPlayer по этому ложному сигналу удалена; аудиодвижок не менялся.

Остаётся дизайн-ревью владельца и проверка на настоящем телефоне перед выкладкой.
Шапка сохраняет существующие входы в настройки/аккаунт. Флаг выключен по умолчанию.

Проверки среза 10.09.2026: оба typecheck — exit 0; webapp unit — 834 passed;
полный функциональный Playwright — 299 passed, 7 штатных skip, exit 0 (6.7 мин).
В числе успешных — все 12 calm-сценариев. Мутация с удалённым вызовом copyTrack
дала ожидаемый красный результат, после чего обработчик восстановлен.
Кадры: `output/playwright/calm/real-{320,390,411}-{aurora-field,pastel}.png`
и `real-custom.png`; на них моковые станции/metadata/аудио, настоящие компоненты
и тема. Живой каталог отдельно открыт в dev-браузере по `?calm=1`.
Полную инвентаризацию проекта повторять не нужно.

### Поправка после отклонённого первого среза

Владелец отклонил `f35f610`: упрощение потеряло стекло и исследовательский смысл.
В `?calm=1` добавлен выбор страны с реальными станциями из уже загруженного
summary; смена страны только меняет предложения. Нажатие на станцию включает
именно её, переход в Глобус сохраняет текущий эфир. Никаких новых запросов для
Главной, ранжировщика похожих треков или выдуманных географических меток нет.
Три сохранённые находки показывают настоящее название/источник; если станция
есть в knownStations, можно включить её снова. Внешние сервисы остаются в
существующем списке находок, доступном через «Разобрать находки».

Стеклянные плеер, навигация и управление используют прозрачность, размытие,
насыщенность и светлую кромку. Убрана плотная заливка всей Главной поверх
пользовательской картинки. У preview отключена старая анимация входа:
она оставляла `filter: blur(0px)` на родителе, и этот backdrop root не давал
панелям размывать wallpaper. У вкладок убрано отдельное вложенное размытие.
Это веб-материал на CSS, не нативный API Liquid Glass.

Дополнительные проверки: выбор страны без смены потока, явный запуск выбранной
станции, Глобус без смены потока, возврат к источнику реальной сохранённой находки,
отсутствие фильтра на родителе стекла и непрозрачной заливки всей Главной.
Снимок с немоковым каталогом: `output/playwright/setup/discovery-catalogue.png`.
Приёмка красоты остаётся за владельцем; прохождение тестов её не заменяет.

Проверки этой итерации: оба typecheck — exit 0; 834 webapp unit — passed;
общий функциональный Playwright — 301 passed / 7 штатных skip, exit 0.
После последней правки облегчённого материала calm-набор — 14 passed, exit 0.
В браузере отдельно проверены: уменьшение прозрачности даёт плотную подложку,
low-power сохраняет облегчённое размытие. Полные pixel-baselines не обновлялись.

### Текущий срез: Лента как раскрытый плеер

Владелец снова отклонил плоское оформление и предложил использовать свайповую
Ленту вместо отдельного полноэкранного плеера. В `?calm=1` вход из мини-плеера
и кнопки играющей рекомендации теперь ведёт в Ленту на выбранной станции.
Вход сохраняет поток; явный свайп использует прежний механизм переключения.
Мини-плеер в Ленте скрыт. Старый FullPlayer остаётся для других входов и пути
без флага; аудиодвижок не переписывался.

У карточки Ленты есть закладка достоверно известного трека. Кнопка «Ещё»
открывает `FeedPlayerTools`: действующий таймер 15/30/45/60 минут, отмена,
громкость, добавление в очередь, переходы к очереди/находкам, поделиться,
открыть поток, детали и скрытие станции. Таймер также доступен из шапки и
сохраняется при закрытии Ленты. В общем `sleepTimer.start` начальное remainingMs
задаётся сразу: больше нет промежуточного отображения 0:00. Лоток использует
существующий useDialog; клавиши громкости не передаются свайповому пейджеру.

На Главной усилены блики, цветные акценты, прозрачность и контактные тени.
Жанровые карточки предлагают конкретную станцию и дальнейший поиск. Направление
появляется по тегам реально загруженного каталога, повтор станции не допускается;
из пула убираются скрытые и известные как недоступные станции. Это не обещание
сходства с треком и не новый ранжировщик. На немоковом каталоге заметны слабые
метаданные (например, новостное название с тегом pop); качество отбора остаётся
ограничением и не подтверждается функциональными тестами.

Проверены кадры Aurora, Neon, Sunset, светлой темы и собственной картинки,
созданной через настоящий редактор Theme Studio. Кадры интерфейса с моками:
`output/playwright/calm/home-*.png`, `tools-*.png`, `feed-player.png`.
Главная с живым локальным каталогом: `output/playwright/setup/feed-home-catalogue.png`.
Картинка пользователя остаётся за интерфейсом. Палитра иллюстрированной карточки
и сцены Ленты самостоятельная; органы управления используют токены выбранной темы.

Следующее действие владельца: оценить Главную и путь мини-плеер → Лента →
свайп/таймер на 5184 с `?calm=1`. Не выкладывать без оценки композиции и проверки
на устройстве. Полные pixel-baselines не обновлялись.

Проверки текущего среза: typecheck и typecheck:test — exit 0; webapp unit —
836 passed, exit 0; полный функциональный Playwright — 305 passed / 7 штатных
skip, exit 0 (6.9 мин), включая 18 calm-сценариев. В браузере на живом каталоге
проверен также Escape: закрывает лоток, оставляя Ленту открытой; закрытие Ленты
возвращает Главную. Моковое аудио в e2e не доказывает фоновое воспроизведение.
